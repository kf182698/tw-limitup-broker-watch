name: Daily Stock Watch (台灣時間 18:00 執行)

on:
  schedule:
    - cron: '0 10 * * 1-5'   # 台灣時間 18:00 (UTC+8 → UTC 10:00)
  workflow_dispatch:

jobs:
  run-daily-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: 檢查程式碼
        uses: actions/checkout@v4

      - name: 設定 Python 環境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安裝專案依賴套件
        run: pip install -r requirements.txt

      - name: 建立數據目錄及必備空檔，防止 workflow 失敗
        run: |
          mkdir -p data_raw data_clean
          touch data_raw/.gitkeep
          touch data_clean/.gitkeep

      - name: 執行漲停券商監控與 Email 通知
        env:
          EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
        run: |
          export TZ='Asia/Taipei'
          TODAY_DATE=$(date +%Y-%m-%d)
          echo "台灣時間 $TODAY_DATE 18:00 開始執行"
          python -m src.app.main run --date $TODAY_DATE

      - name: 驗證數據文件是否生成
        run: |
          echo "檢查 data_raw 目錄:"
          ls -la data_raw/ 2>/dev/null || echo "data_raw 不存在或為空"
          echo ""
          echo "檢查 data_clean 目錄:"
          ls -la data_clean/ 2>/dev/null || echo "data_clean 不存在或為空"
          echo ""
          echo "Git 狀態："
          git status

      - name: 自動 Commit 結果
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_options: --allow-empty
          file_pattern: data_raw/* data_raw/.gitkeep data_clean/* data_clean/.gitkeep
          commit_message: "🤖 數據自動提交：更新每日監控結果"
          skip_dirty_check: true
          repository: .
          commit_user_name: github-actions[bot]
          commit_user_email: 41898282+github-actions[bot]@users.noreply.github.com
          commit_author: "*** <221647913+***@users.noreply.github.com>"
          skip_fetch: false
          skip_checkout: false
          disable_globbing: false
          create_branch: false
          create_git_tag_only: false
          internal_git_binary: git
